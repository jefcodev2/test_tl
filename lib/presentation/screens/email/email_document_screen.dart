import 'package:flutter/material.dart';

class EmailDocumentScreen extends StatelessWidget {
  const EmailDocumentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}